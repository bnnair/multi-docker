language: generic

sudo: required
services:
  - docker


before_install:
  - docker build -t bnnair/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true bnnair/react-test npm test